spec_version: 2
description: Testing ansible grain.
inputs:
  instance_ip:
    type: string
    default: 0.0.0.0
outputs: {}
grains:
  # install-apache:
  #   kind: ansible
  #   spec:
  #     source:
  #       store: iac-assets
  #       path: ansible/install-apache.yaml
  #     agent:
  #       name: adoc-vcenter
  #     inventory-file:
  #       all:
  #         hosts:
  #           '{{ .inputs.instance_ip }}':
  #         vars:
  #           ansible_user: centos
  #           ansible_ssh_private_key_file: /var/secrets/torque.pem
  
  # hello-world:
  #   kind: ansible
  #   spec:
  #     source:
  #       store: iac-assets
  #       path: ansible/hello-world.yaml
  #     agent:
  #       name: adoc-ec2-k8s
  #     inventory-file:
  #       all:
  #         hosts:
  #           host1:
  #             person_name: John
  #           host2:
  #         vars:
  #           person_name: John

  # local-task:
  #   kind: ansible
  #   spec:
  #     source:
  #       store: iac-assets
  #       path: ansible/local-task.yaml
  #     agent:
  #       name: adoc-ec2-k8s
  #     inventory-file:
  #       all:
  #         hosts:
  #           local:
  #             ansible_connection: local
  #             ansible_host: localhost

  download-file-s3:
    kind: ansible
    spec:
      source:
        store: iac-assets
        path: ansible/download-file-s3.yaml
      agent:
        name: adoc-ec2-k8s
      inventory-file:
        all:
          hosts:
            local:
              ansible_connection: local
              ansible_host: localhost
          vars:
            aws_access_key: AKIA563JS5T2E3AVOD5L
            aws_secret_access_key: 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
